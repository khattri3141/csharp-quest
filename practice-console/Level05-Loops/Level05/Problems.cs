namespace Level05;

// =======================================================
// Practice problems — LeetCode style.
//
// Solve here, prove it with `dotnet test`, then open the
// linked problem on LeetCode and submit the same idea there.
// =======================================================
public static class Problems
{
    // Problem: Fizz Buzz  (LeetCode #412, Easy)
    // https://leetcode.com/problems/fizz-buzz/
    //
    // The most famous interview warm-up there is.
    // Return a list of length n. For each i from 1 to n:
    //   - "FizzBuzz" if i is divisible by BOTH 3 and 5
    //   - "Fizz"     if i is divisible by 3
    //   - "Buzz"     if i is divisible by 5
    //   - otherwise  the number itself as text, e.g. "1"
    //
    // FizzBuzz(5) -> ["1", "2", "Fizz", "4", "Buzz"]
    // Hint: check divisible-by-15 (or by 3 AND 5) FIRST,
    //       then by 3, then by 5, else the number.
    public static List<string> FizzBuzz(int n)
    {
        return new List<string>(); // change me
    }
}
