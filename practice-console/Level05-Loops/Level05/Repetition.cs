namespace Level05;

// =======================================================
// Level 5 — Loops
//
// Do the same work many times without copy-paste.
// =======================================================
public static class Repetition
{
    // TODO #1
    // Add up every whole number from 1 to n (inclusive).
    //   SumToN(5) -> 1+2+3+4+5 = 15
    //   SumToN(1) -> 1
    // Use a for loop and a running total variable.
    public static int SumToN(int n)
    {
        return 0; // change me
    }

    // TODO #2
    // Count down from n to 1, then add the word "Liftoff!".
    //   Countdown(3) -> ["3", "2", "1", "Liftoff!"]
    //   Countdown(1) -> ["1", "Liftoff!"]
    // Add each number AS TEXT to the list (use .ToString() or
    // string interpolation), then add "Liftoff!" at the end.
    public static List<string> Countdown(int n)
    {
        return new List<string>(); // change me
    }
}
