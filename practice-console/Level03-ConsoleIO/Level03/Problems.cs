namespace Level03;

// =======================================================
// Practice problems — LeetCode style.
//
// Solve here, prove it with `dotnet test`, then open the
// linked problem on LeetCode and submit the same idea there.
// =======================================================
public static class Problems
{
    // Problem: Defanging an IP Address  (LeetCode #1108, Easy)
    // https://leetcode.com/problems/defanging-an-ip-address/
    //
    // A "defanged" IP address replaces every period "." with "[.]"
    // so it cannot be clicked as a link.
    //   "1.1.1.1"      -> "1[.]1[.]1[.]1"
    //   "255.100.50.0" -> "255[.]100[.]50[.]0"
    // Hint: every string has a .Replace(oldText, newText) method.
    public static string DefangIp(string address)
    {
        return ""; // change me
    }
}
